class Solution{
public:
    int sumOfDigits(int n){
          int r,sum=0;
        while(n>0)
        {
            r=n%10;
            sum=sum + r;
            n=n/10;
            
        }
        return sum;
    }
};
